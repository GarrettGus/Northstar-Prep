DATABASE_URL=
SESSION_SECRET=
# Used once by `npm run db:migrate` to create the first owner account when no
# accounts exist yet. Safe to leave set afterward.
HOUSEHOLD_OWNER_EMAIL=
HOUSEHOLD_PASSWORD=
